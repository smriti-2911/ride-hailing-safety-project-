# Copy to .env.local for local dev. Vite reads VITE_* at build time for production.

# Backend API (no trailing slash). Production: your deployed API HTTPS origin.
VITE_API_BASE_URL=http://localhost:5001
